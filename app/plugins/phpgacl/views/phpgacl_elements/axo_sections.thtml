<?php echo $this->renderElement('phpgacl_start'); ?>

<?php if (isset($records) && count($records) > 0): ?>
	<p>
	Here is the list of your Controllable Element sections. Each Controllable Element section can have any number of Controllable Elements.
	You can <?php echo $phpgaclHtml->link('Add a new Controllable Element section', $plugin_url . '/phpgaclElements/edit_axo_section'); ?>
	</p>
	
	<table class="phpgacl-listing">
		<thead>
			<tr>
				<th>Identifier</th>
				<th>Descriptive Name</th>
				<th>Objects</th>
				<td></td>
			</tr>
		</thead>
		<tbody>
			<?php foreach($records as $index => $record): ?>
				<tr class="<?php echo ($index % 2 == 0 ? 'even' : 'odd'); ?>">
					<td><?php echo $record['PhpgaclAxoSection']['value']; ?></td>
					<td><?php echo $record['PhpgaclAxoSection']['name']; ?></td>
					<td><?php echo $phpgaclHtml->link((count($record['PhpgaclAxo']) > 0 ? count($record['PhpgaclAxo']) . ' Object' . (count($record['PhpgaclAxo']) > 1 ? 's' : '') : 'Add Object'), $plugin_url . '/phpgaclElements/axos/' . $record['PhpgaclAxoSection']['value']); ?></td>
					<td>
						<?php echo $phpgaclHtml->link('Edit', $plugin_url . '/phpgaclElements/edit_axo_section/' . $record['PhpgaclAxoSection']['value']); ?>
						<?php if (!isset($record['protected']) || !$record['protected']): ?>
							-
							<?php echo $phpgaclHtml->link('Delete', $plugin_url . '/phpgaclElements/delete_axo_section/' . $record['PhpgaclAxoSection']['value']); ?>
						<?php endif; ?>
					</td>
				</tr>
			<?php endforeach; ?>
		</tbody>
	</table>
<?php else: ?>
	<p>
	There are no Controllable Element sections defined. You can start by <?php echo $phpgaclHtml->link('Adding a new Controllable Element section', $plugin_url . '/phpgaclElements/edit_axo_section'); ?>
	</p>
<?php endif; ?>

<?php echo $this->renderElement('phpgacl_end'); ?>